#!/bin/sh

set -eu

main () {
    local deps='watchdog mock pytest'
    virtualenv venv
    ./venv/bin/pip install -U pip setuptools
    ./venv/bin/pip install $deps

    virtualenv venv3 -p $(which python3)
    ./venv3/bin/pip install -U pip setuptools
    ./venv3/bin/pip install $deps
}

main
