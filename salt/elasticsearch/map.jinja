{% set pillar_get = salt['pillar.get'] %}

{% set elasticsearch = salt['grains.filter_by']({
    'default': {
        'initial_master_nodes': [grains.id],
        'version': '7.7.1'
    },
}, merge=pillar_get('elasticsearch')) %}
